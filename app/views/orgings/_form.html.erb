<%= form_with(model: orging, local: true) do |form| %>
  <% if orging.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(orging.errors.count, "error") %> prohibited this orging from being saved:</h2>

      <ul>
      <% orging.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
